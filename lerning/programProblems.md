# 问题1：add student功能在等待输入age时，如果输入的不是数字，这个功能没有正确处理这个问题，而是默认添加成功，且age的值默认为0，score由于没有正确输入，所以是野值。

# 问题2：上述问题导致分数未能正确初始化，所以在程序加载时stod这个函数会抛出异常，异常没有捕获处理导致程序崩溃。

# 问题3：删除学生后有些id会空着，需要优先把前面的已删除导致空着的id再次优先分配出去，而不是一直累加。
